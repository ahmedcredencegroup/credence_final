# Contact form delivery
# Google Apps Script web-app URL that receives contact-form submissions and
# emails each one to komal_russell@credencegroup.co. Deploy the script (see
# docs/contact-form-setup.md), then paste its /exec URL here.
# Also set this same variable in Netlify: Site settings > Environment variables.
VITE_CONTACT_ENDPOINT=
